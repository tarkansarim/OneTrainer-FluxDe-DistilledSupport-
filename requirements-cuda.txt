# pytorch
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124
torchvision==0.21.0+cu124
onnxruntime-gpu==1.22.0
# Match torch 2.6.0+cu124's expected NCCL to avoid resolver conflicts on Linux
nvidia-nccl-cu12==2.21.5; sys_platform == "linux"
triton-windows==3.4.0.post20; sys_platform == "win32"

# optimizers
bitsandbytes==0.46.0 # bitsandbytes for 8-bit optimizers and weight quantization
